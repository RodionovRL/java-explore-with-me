version: '3.1'
services:
  stats-server:
    build: ./stats/server
    container_name: stats_server
    ports:
      - "9090:9090"
    depends_on:
      - stats-db
    environment:
      - DB_NAME=statsdb
      - POSTGRES_USER=ewm
      - POSTGRES_PASSWORD=ewm_pwd
      - DB_HOST=ewm_stats_db
      - DB_PORT=5432

  stats-db:
    image: postgres:14-alpine
    container_name: ewm_stats_db
    ports:
      - "6541:5432"
    environment:
      - POSTGRES_DB=statsdb
      - POSTGRES_USER=ewm
      - POSTGRES_PASSWORD=ewm_pwd

  ewm-service:
    build: ./main
    container_name: ewm_service
    ports:
      - "8080:8080"
    environment:
      - STATS_SERVER_URL=http://ewm_db:9090

  ewm-db:
    image: postgres:14-alpine
    container_name: ewm_db
